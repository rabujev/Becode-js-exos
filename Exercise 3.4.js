const readlineSync = require("readline-sync");
let array1 = [10, 55, 100];  //We define the array//
console.log("The maximum element in the array is " + Math.max(...array1) + ", the min element is " + Math.min(...array1));
//J'utilise ici les fonctions Math.max() et Math.min() : https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math/max
